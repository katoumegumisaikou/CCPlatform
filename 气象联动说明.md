# 气象联动模块

## 概述

基于拓达威光伏清扫机器人云控平台（CCPlatform）的现有架构，新增了**气象联动**功能模块。该模块接入和风天气 API，实现气象数据实时获取、缓存、可视化展示以及智能清扫决策辅助。

---

## 一、模块架构

```
前端 (React)                     后端 (Go)                     外部服务
┌────────────────┐    HTTP     ┌──────────────────┐    HTTPS    ┌──────────────┐
│ Weather/       │ ←──────────→ │ WeatherHandler   │ ←─────────→ │ 和风天气 API  │
│ Warnings/      │             │    │              │            │ v7            │
│ History/       │             │ WeatherService   │            │ (专属 Host)    │
│ Decisions/     │             │    │              │            └──────────────┘
│                │             │ WeatherRepo      │
│ api/weather.ts │             │    │              │
│ api/cleaning-  │             │ Weather Models   │ ←──→ MySQL (10 张新表)
│   Decision.ts  │             └──────────────────┘
└────────────────┘
```

---

## 二、新增文件清单

### 后端 (Go) — 14 个新文件

| 文件路径 | 说明 |
|---------|------|
| `pkg/heweather/models.go` | 和风天气 API v7 响应结构体（13 个 struct） |
| `pkg/heweather/client.go` | 和风天气 HTTP 客户端（Web API 认证方式） |
| `internal/model/weather_cache.go` | 天气缓存表模型（5 张表） |
| `internal/model/micro_weather.go` | 微气象站设备与观测数据模型（2 张表） |
| `internal/model/cleaning_decision.go` | 清扫决策规则与频次调整模型（3 张表） |
| `internal/repository/weather_repo.go` | 天气数据访问层（12 个方法） |
| `internal/repository/cleaning_decision_repo.go` | 决策数据访问层（12 个方法） |
| `internal/service/weather_service.go` | 天气业务逻辑：API 拉取、缓存刷新、查询 |
| `internal/service/cleaning_decision_service.go` | 决策引擎：气象评估、灰尘评分、频次分析 |
| `internal/handler/weather_handler.go` | 天气 HTTP 处理器（7 个端点） |
| `internal/handler/cleaning_decision_handler.go` | 决策 HTTP 处理器（12 个端点） |
| `internal/router/weather.go` | 天气 + 决策路由注册 |

### 后端 — 4 个修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `config/config.yaml` | 新增 `heweather` 配置节（Token、API Host、刷新间隔） |
| `internal/config/config.go` | 新增 `HeWeatherConfig` 结构体 |
| `internal/repository/db.go` | `AutoMigrate` 新增 10 张表 |
| `internal/middleware/rbac.go` | 所有角色添加 `weather` 模块权限 |
| `internal/router/router.go` | 注册 `WeatherHandler` 和 `CleaningDecisionHandler` |

### 前端 (React/TypeScript) — 7 个新文件

| 文件路径 | 说明 |
|---------|------|
| `frontend/src/api/weather.ts` | 天气 API 调用模块（7 个方法） |
| `frontend/src/api/cleaningDecision.ts` | 决策 API 调用模块（12 个方法） |
| `frontend/src/pages/Weather/index.tsx` | 气象仪表盘（实时天气 / 7天预报 / 逐小时） |
| `frontend/src/pages/Weather/Warnings.tsx` | 预警看板（当前预警 / 历史查询） |
| `frontend/src/pages/Weather/History.tsx` | 历史查询（趋势图 / 预警记录） |
| `frontend/src/pages/Weather/Decisions.tsx` | 清扫决策（建议 / 规则管理 / 频次调整） |

### 前端 — 3 个修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `frontend/src/types/index.ts` | 新增 9 个 TypeScript 类型定义 |
| `frontend/src/App.tsx` | 新增 4 条路由（weather / warnings / history / decisions） |
| `frontend/src/components/Layout/AppLayout.tsx` | 新增「气象中心」侧边栏菜单（含面包屑） |

---

## 三、数据库设计（10 张新表）

| 表名 | 主键 | 用途 |
|------|------|------|
| `weather_now_cache` | `location_id` | 实时天气缓存，按坐标 UPSERT |
| `weather_daily_cache` | `(location_id, fx_date)` | 7 天逐天预报缓存 |
| `weather_hourly_cache` | `(location_id, fx_time)` | 逐小时预报缓存（24h） |
| `weather_warnings` | `(location_id, warning_id)` | 天气预警记录（追加模式） |
| `weather_air_quality` | `location_id` | 空气质量缓存（需付费套餐） |
| `micro_weather_stations` | `station_code` | 微气象站设备（硬件预留） |
| `micro_weather_data` | `id` (自增) | 微气象站观测数据 |
| `cleaning_decision_rules` | `rule_id` | 清扫决策规则（气象条件阈值 + 灰尘规则） |
| `extreme_weather_rules` | `rule_id` | 极端天气应对规则 |
| `cleaning_schedule_adjustments` | `id` (自增) | 动态频次调整建议记录 |

所有表通过 `station_id`（varchar(32)，带索引）与电站关联。

---

## 四、API 端点设计（19 个）

### 气象数据

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/v1/weather/now` | 登录即可 | 获取实时天气 |
| POST | `/api/v1/weather/refresh` | `weather:manage` | 手动刷新天气数据 |
| GET | `/api/v1/weather/forecast/daily` | 登录即可 | 获取 7 天逐天预报 |
| GET | `/api/v1/weather/forecast/hourly` | 登录即可 | 获取逐小时预报 |
| GET | `/api/v1/weather/warnings` | 登录即可 | 获取当前生效预警 |
| GET | `/api/v1/weather/warnings/history` | 登录即可 | 历史预警分页查询 |
| GET | `/api/v1/weather/air-quality` | 登录即可 | 获取空气质量 |

### 清扫决策

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/v1/cleaning-decisions/rules` | 登录即可 | 决策规则列表 |
| POST | `/api/v1/cleaning-decisions/rules` | `weather:manage` | 创建决策规则 |
| GET | `/api/v1/cleaning-decisions/rules/:id` | 登录即可 | 规则详情 |
| PUT | `/api/v1/cleaning-decisions/rules/:id` | `weather:manage` | 更新规则 |
| DELETE | `/api/v1/cleaning-decisions/rules/:id` | `weather:manage` | 删除规则 |
| GET | `/api/v1/cleaning-decisions/extreme-rules` | 登录即可 | 极端天气规则列表 |
| POST | `/api/v1/cleaning-decisions/extreme-rules` | `weather:manage` | 创建极端规则 |
| PUT | `/api/v1/cleaning-decisions/extreme-rules/:id` | `weather:manage` | 更新极端规则 |
| DELETE | `/api/v1/cleaning-decisions/extreme-rules/:id` | `weather:manage` | 删除极端规则 |
| GET | `/api/v1/cleaning-decisions/advice` | 登录即可 | 获取智能清扫建议 |
| GET | `/api/v1/cleaning-decisions/adjustments` | 登录即可 | 频次调整记录 |
| POST | `/api/v1/cleaning-decisions/adjustments` | `weather:manage` | 生成频次分析 |

---

## 五、实现的功能

### 5.1 气象数据采集

- ✅ 接入和风天气 API v7，支持实时天气、7 天预报、24 小时逐小时预报、天气预警
- ✅ 使用专属 API Host + Web API Key 认证
- ✅ 并发拉取 5 个 API 接口，提高刷新效率
- ✅ 数据缓存到 MySQL，按电站关联
- ✅ 手动刷新 + 预留定时刷新接口

### 5.2 气象数据可视化

- ✅ **气象仪表盘**：实时天气统计卡片（温度/风力/湿度/能见度）+ 详情面板
- ✅ **7 天预报**：ECharts 温度柱状图 + 降水折线图 + 详情表格
- ✅ **逐小时预报**：ECharts 温湿度曲线 + 横向滚动小时卡片
- ✅ **空气质量**：ECharts 仪表盘 + 污染物详情（需付费套餐）
- ✅ **预警看板**：当前生效预警卡片 + 历史预警分页查询
- ✅ **历史查询**：预报趋势图 + 预警记录表格

### 5.3 智能清扫决策

- ✅ **决策引擎**：根据气象条件（风速/温度/湿度/降水/能见度）和灰尘累积评分，综合生成清扫建议
- ✅ **清扫建议**：建议清扫 / 暂缓清扫 / 暂停清扫 + 决策理由 + 推荐时间窗口
- ✅ **灰尘评分**：基于距上次清扫天数 × 灰尘累积率 / 灰尘阈值
- ✅ **决策规则管理**：支持创建/编辑/删除气象条件阈值规则
- ✅ **极端天气规则**：支持大风/暴雨/暴雪/高温/沙尘暴等极端天气的自动响应规则
- ✅ **动态频次调整**：根据 7 天预报分析适宜清扫天数，给出频次建议（旱季/雨季/正常模式）

---

## 六、配置说明

```yaml
# config/config.yaml
heweather:
  api_token: "你的和风天气 API Key"
  base_url: "https://你的专属Host/v7/"     # 从 https://console.qweather.com/setting/ 获取
  air_url: "https://你的专属Host/v7/air/now"
  refresh_interval_min: 30                  # 自动刷新间隔（分钟）
```

> **重要**：和风天气已废弃通用域名 `devapi.qweather.com`，必须使用控制台分配的专属 API Host。

---

## 七、关键技术要点

1. **认证方式**：Web API 类型使用 `?key=` URL 参数认证（非 JWT Bearer）
2. **坐标格式**：和风天气 API 的 `location` 参数使用 `"经度,纬度"` 格式（保留 2 位小数）
3. **并发拉取**：5 个 API 接口使用 goroutine 并发调用，提高刷新速度
4. **UPSERT 策略**：使用 GORM `clause.OnConflict{UpdateAll: true}` 实现主键冲突时覆盖写入
5. **权限控制**：查看权限对所有已登录用户开放（`weather:view`），管理操作仅 sys_admin 和 station_mgr 可执行（`weather:manage`）
6. **错误边界**：前端使用 React ErrorBoundary 防止渲染崩溃导致白屏
7. **免费套餐限制**：空气质量 API 在免费套餐下返回 403，已做优雅降级处理

---
